<form action="/" method="POST">
	<input id="urlToShorten" name="urlToShorten" type="text">
	<button>Shorten URL</button>
	  {{#each urls as |url|}}
	  	<a href="/key/{{url}}">{{url}}</a>
	  {{/each}}
</form>
